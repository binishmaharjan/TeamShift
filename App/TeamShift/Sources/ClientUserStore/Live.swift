import Dependencies
import FirebaseFirestore
import Foundation
import SharedModels

extension UserStoreClient {
    actor Session {
        func saveUserToStore(withUser user: AppUser) async throws {
//            let reference = Firestore.firestore().collection("users").document(user.id)
        }
    }
}
